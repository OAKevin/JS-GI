var name1 = "Kevin"
var name2 = "Romeo Santos"

if (name1.length > name2.length) {
    let remainder = name1.length - name2.length 
    console.log("The name "+ name1 + " is longer than "+name2+ " by "+ remainder +" characters.")
}else {
    let remainder = name2.length - name1.length 
    console.log("The name "+ name2 + " is longer than "+name1+ " by "+ remainder +" characters.")
}

